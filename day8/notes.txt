DAY 8 - CI/CD Pipeline
This is on day8-cicd branch


DAY 8 - CI/CD & GitHub Actions

CI = Continuous Integration (auto test on push)
CD = Continuous Deployment (auto deploy after test)

# Pipeline file location:
.github/workflows/pipeline.yml

# Trigger on push:
on:
  push:
    branches:
      - day8-cicd

# Pipeline structure:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: step name
        run: command
